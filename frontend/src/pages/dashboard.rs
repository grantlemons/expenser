use leptos::*;

#[component]
pub fn DashboardPage(cx: Scope) -> impl IntoView {
    view! { cx,
      <div class="flex">
        <section class="left-section">
        </section>
        <section class="right-section">
        </section>
      </div>
    }
}
